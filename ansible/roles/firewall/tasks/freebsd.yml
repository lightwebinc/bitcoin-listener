---
- name: Enable pf in rc.conf (FreeBSD)
  ansible.builtin.lineinfile:
    path: /etc/rc.conf
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
  loop:
    - { regexp: '^pf_enable=', line: 'pf_enable="YES"' }
    - { regexp: '^pflog_enable=', line: 'pflog_enable="YES"' }

- name: Deploy shard-listener pf ruleset fragment
  ansible.builtin.template:
    src: shard-listener.pf.conf.j2
    dest: /etc/pf.anchors/shard-listener
    owner: root
    group: wheel
    mode: "0644"
  notify: Reload pf

- name: Ensure /etc/pf.conf references the shard-listener anchor
  ansible.builtin.blockinfile:
    path: /etc/pf.conf
    create: true
    mode: "0644"
    marker: "# {mark} shard-listener anchor"
    block: |
      anchor "shard-listener"
      load anchor "shard-listener" from "/etc/pf.anchors/shard-listener"
  notify: Reload pf

- name: Enable pf service (FreeBSD)
  ansible.builtin.service:
    name: pf
    enabled: true
    state: started
